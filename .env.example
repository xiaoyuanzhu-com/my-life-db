# MyLifeDB Environment Variables

# Authentication (Optional)
# Set a password to protect your application with login
# If not set or empty, the app will be accessible without authentication
# MLD_PASSWORD=your-secure-password-here

# Data Directory
# Directory where all your data is stored (defaults to ./data)
# MY_DATA_DIR=./data
